const multiply = (a: number, b: number): number => a * b;

const greetin = (name: string): string => `Olá ${name}`;

console.log(multiply(2, 3));
console.log(greetin("Kayque"));

//use o comando npm run dev para gerar o arquivo main.js
//use o comando node main.js para executar o arquivo main.js
